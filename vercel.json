{
  "buildCommand": "curl -sS https://getcomposer.org/installer | php && php composer.phar install --no-dev --optimize-autoloader && php artisan config:cache && php artisan route:cache && php artisan view:cache",
  "outputDirectory": "public",
  "framework": null,
  "installCommand": "curl -sS https://getcomposer.org/installer | php",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.php"
    }
  ]
}